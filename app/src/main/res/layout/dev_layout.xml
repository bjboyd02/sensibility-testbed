<?xml version="1.0" encoding="utf-8"?>

<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
			xmlns:tools="http://schemas.android.com/tools"
			android:layout_width="fill_parent"
	android:layout_height="wrap_content"
	android:theme="@android:style/Theme.Holo.Light.DarkActionBar"
	android:fillViewport="true" >
<LinearLayout
	xmlns:android="http://schemas.android.com/apk/res/android"
	android:layout_width="fill_parent"
	android:layout_height="fill_parent"
	android:orientation="vertical"
	android:id="@+id/root"
	android:gravity="center"
	android:weightSum="1">

	<EditText
		android:id="@+id/url_edittext"
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:inputType="textUri"
		android:ems="10"
		tools:text="Installer download URL..."
		android:contentDescription="Input an installer download URL here, or leave blank to use the default value. (Continuing with the default value will not let you access this device though!)"/>



	<Button
		android:layout_marginTop="10sp"
		android:id="@+id/install_seattle"
		android:layout_width="290dp"
		android:layout_height="wrap_content"
		android:text="Install (optional url)"
		android:textSize="20sp"
		android:layout_below="@+id/view"
		android:textAllCaps="false"
		android:layout_alignParentStart="true">
	</Button>


	<Button
		android:id="@+id/start"
		android:layout_width="290dp"
		android:layout_height="wrap_content"
		android:text="Start"
		android:textSize="20sp"
		android:layout_marginTop="30sp"

		android:layout_below="@+id/view"
		android:textAllCaps="false"
		android:layout_alignParentStart="true">
	</Button>
	<Button
		android:id="@+id/kill"
		android:layout_width="290dp"
		android:layout_marginTop="15sp"
		android:layout_height="wrap_content"
		android:text="Kill"
		android:textSize="20sp"
		android:layout_below="@+id/view"
		android:textAllCaps="false"
		android:layout_alignParentStart="true">
	</Button>

	</LinearLayout>
</ScrollView>